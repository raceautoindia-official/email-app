// import { Link } from 'react-router-dom';

function Header() {
  return (
    <header>
      <nav>
        {/* <Link to="/emails">Email List</Link> | <Link to="/compose">Compose Email</Link> |{' '} */}
        {/* <button onClick={() => {
          localStorage.removeItem('token');
          window.location.href = '/';
        }}>
          Logout
        </button> */}
      </nav>
    </header>
  );
}

export default Header;
